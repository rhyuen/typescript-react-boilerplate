import * as React from 'react';
import styled from 'styled-components';

const TwoCol: React.FunctionComponent = styled.div`
   grid-column: span 6;
   min-height: 75vh;
   padding: 0 10px;
`;

export default TwoCol;