import * as React from "react";

const Home: React.FunctionComponent<{}> = () => {
  return (
    <div style={{ background: "pink" }}>
      User home page for dynamically generated values.
    </div>
  );
};

export default Home;
